---
import { Image } from 'astro:assets';
import homeScreenshot from '@/assets/screenshots/home.webp';
import loginScreenshot from '@/assets/screenshots/login.webp';
import profileTopArtistsScreenshot from '@/assets/screenshots/profile_top_artists.webp';
import profileTopSongsScreenshot from '@/assets/screenshots/profile_top_songs.webp';

const GITHUB = 'https://github.com/mliem2k/playtivity';
const DOWNLOAD = 'https://github.com/mliem2k/playtivity/releases/latest';

const features = [
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>`,
    title: 'Friend Activity Feed',
    desc: 'See exactly what your Spotify friends are listening to, updated live every 30 seconds.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>`,
    title: 'Profile Dashboard',
    desc: 'Your currently playing track, all-time top songs, and top artists in one clean view.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="2" ry="2"/><line x1="12" y1="18" x2="12.01" y2="18"/></svg>`,
    title: 'Home Screen Widget',
    desc: 'Keep tabs on your friends without opening the app — right from your Android home screen.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>`,
    title: 'Tap to Play',
    desc: "Tap any track a friend is listening to and it launches instantly in Spotify.",
  },
];

const steps = [
  { n: '1', title: 'Install the APK', body: 'Download the latest release and install it on your Android device. No Play Store needed.' },
  { n: '2', title: 'Log in with Spotify', body: 'Authenticate securely through Spotify\'s official web login — Playtivity never stores your password.' },
  { n: '3', title: 'See your friends\' music', body: 'Your friend activity feed loads instantly and refreshes automatically in the background.' },
];
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Playtivity — See what your friends are listening to</title>
  <meta name="description" content="Playtivity brings Spotify's Friend Activity to your Android home screen. See what your friends are listening to in real-time." />
  <meta property="og:title" content="Playtivity — See what your friends are listening to" />
  <meta property="og:description" content="Real-time Spotify friend activity for Android. Free and open source." />
  <meta property="og:image" content={homeScreenshot.src} />
  <link rel="icon" type="image/webp" href="/playtivity_logo.webp" />
  <link rel="preload" href="/fonts/montserrat-700.woff2" as="font" type="font/woff2" crossorigin />
  <link rel="preload" href="/fonts/montserrat-900.woff2" as="font" type="font/woff2" crossorigin />
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-brand">
    <img src="/playtivity_logo.webp" alt="Playtivity" width="32" height="32" />
    <span>Playtivity</span>
  </a>
  <div class="nav-links">
    <a href="#features">Features</a>
    <a href="#how-it-works">How it works</a>
    <a href={GITHUB} target="_blank" rel="noopener" class="nav-github">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
      GitHub
    </a>
    <button type="button" class="btn-primary btn-sm dl-apk">Download APK</button>
  </div>
</nav>

<main>
<!-- HERO -->
<section class="hero">
  <div class="hero-content">
    <div class="hero-text">
      <div class="equalizer" aria-hidden="true">
        <span></span><span></span><span></span><span></span><span></span>
      </div>
      <h1>See what your friends<br />are listening to</h1>
      <p class="hero-sub">Playtivity brings Spotify's Friend Activity to your Android device — live feed, home screen widget, and profile stats. Free and open source.</p>
      <div class="hero-actions">
        <button type="button" class="btn-primary dl-apk">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
          Download APK
        </button>
        <a href={GITHUB} target="_blank" rel="noopener" class="btn-ghost">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
          View on GitHub
        </a>
      </div>
      <p class="hero-note">Android only &middot; Free &middot; No account required beyond Spotify</p>
    </div>
    <div class="hero-phone">
      <div class="phone-frame">
        <Image src={homeScreenshot} alt="Playtivity friend activity feed" width={320} height={676} loading="eager" fetchpriority="high" />
      </div>
    </div>
  </div>
</section>

<!-- FEATURES -->
<section class="features" id="features">
  <div class="section-inner">
    <h2 class="section-title">Everything you need</h2>
    <p class="section-sub">All the Spotify social features that the official app hides behind a desktop-only sidebar.</p>
    <div class="features-grid">
      {features.map(f => (
        <div class="feature-card">
          <div class="feature-icon" set:html={f.icon} />
          <h3>{f.title}</h3>
          <p>{f.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- SCREENSHOTS -->
<section class="screenshots" id="screenshots">
  <div class="section-inner">
    <h2 class="section-title">Looks right at home</h2>
    <p class="section-sub">Authentic Spotify dark theme. Your data, beautifully presented.</p>
  </div>
  <div class="screenshots-scroll">
    <div class="screenshots-track">
      {[
        { src: homeScreenshot, label: 'Friend Activity' },
        { src: profileTopSongsScreenshot, label: 'Top Songs' },
        { src: profileTopArtistsScreenshot, label: 'Top Artists' },
        { src: loginScreenshot, label: 'Sign in' },
      ].map(s => (
        <div class="screenshot-item">
          <div class="phone-frame phone-frame--sm">
            <Image src={s.src} alt={s.label} width={220} height={464} loading="lazy" />
          </div>
          <span class="screenshot-label">{s.label}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- HOW IT WORKS -->
<section class="how-it-works" id="how-it-works">
  <div class="section-inner">
    <h2 class="section-title">Up and running in minutes</h2>
    <p class="section-sub">No configuration. No subscription. Just install and log in.</p>
    <div class="steps">
      {steps.map(s => (
        <div class="step">
          <div class="step-number">{s.n}</div>
          <div class="step-body">
            <h3>{s.title}</h3>
            <p>{s.body}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- DOWNLOAD CTA -->
<section class="cta">
  <div class="section-inner cta-inner">
    <div class="equalizer equalizer--lg" aria-hidden="true">
      <span></span><span></span><span></span><span></span><span></span>
    </div>
    <h2>Ready to tune in?</h2>
    <p>Download the latest release and see what your friends are listening to right now.</p>
    <button type="button" class="btn-primary btn-lg dl-apk">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
      Download Latest APK
    </button>
    <p class="cta-note">Android &middot; Free &middot; Open source under MIT</p>
  </div>
</section>
</main>

<!-- FOOTER -->
<footer>
  <div class="footer-inner">
    <div class="footer-brand">
      <img src="/playtivity_logo.webp" alt="Playtivity" width="24" height="24" />
      <span>Playtivity</span>
    </div>
    <p class="footer-disclaimer">
      Playtivity uses Spotify's unofficial internal API — the same one used by the Spotify web player.
      This is not affiliated with or endorsed by Spotify. Functionality may change if Spotify updates their API.
    </p>
    <div class="footer-links">
      <a href={GITHUB} target="_blank" rel="noopener">GitHub</a>
      <a href={`${GITHUB}/releases`} target="_blank" rel="noopener">Releases</a>
      <a href={`${GITHUB}/blob/main/README.md`} target="_blank" rel="noopener">Docs</a>
      <a href={`${GITHUB}/issues`} target="_blank" rel="noopener">Issues</a>
    </div>
    <p class="footer-copy">MIT License &middot; Made with love for music lovers</p>
  </div>
</footer>

</body>
</html>

<style is:global>
  @font-face {
    font-family: 'Montserrat';
    font-style: normal;
    font-weight: 400;
    font-display: swap;
    src: url('/fonts/montserrat-400.woff2') format('woff2');
  }
  @font-face {
    font-family: 'Montserrat';
    font-style: normal;
    font-weight: 500;
    font-display: swap;
    src: url('/fonts/montserrat-500.woff2') format('woff2');
  }
  @font-face {
    font-family: 'Montserrat';
    font-style: normal;
    font-weight: 600;
    font-display: swap;
    src: url('/fonts/montserrat-600.woff2') format('woff2');
  }
  @font-face {
    font-family: 'Montserrat';
    font-style: normal;
    font-weight: 700;
    font-display: swap;
    src: url('/fonts/montserrat-700.woff2') format('woff2');
  }
  @font-face {
    font-family: 'Montserrat';
    font-style: normal;
    font-weight: 800;
    font-display: swap;
    src: url('/fonts/montserrat-800.woff2') format('woff2');
  }
  @font-face {
    font-family: 'Montserrat';
    font-style: normal;
    font-weight: 900;
    font-display: swap;
    src: url('/fonts/montserrat-900.woff2') format('woff2');
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg:          #0a0a0a;
    --surface:     #121212;
    --raised:      #181818;
    --elevated:    #282828;
    --green:       #1DB954;
    --green-light: #1ED760;
    --text:        #ffffff;
    --muted:       #a7a7a7;
    --subdued:     #8a8a8a;
    --radius:      12px;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Montserrat', system-ui, sans-serif;
    background: var(--bg);
    color: var(--text);
    line-height: 1.6;
    overflow-x: hidden;
  }

  img { display: block; max-width: 100%; }
  a { color: inherit; text-decoration: none; }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 clamp(1.5rem, 5vw, 4rem);
    height: 64px;
    background: rgba(10,10,10,0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255,255,255,0.06);
  }

  .nav-brand {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 700;
    font-size: 1.05rem;
    letter-spacing: 0.3px;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 3vw, 2rem);
    font-size: 0.875rem;
    font-weight: 500;
  }

  .nav-links a { color: var(--muted); transition: color 0.15s; }
  .nav-links a:hover { color: var(--text); }

  .nav-github {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  @media (max-width: 600px) {
    .nav-links a:not(.btn-primary):not(.nav-github) { display: none; }
    .nav-github span { display: none; }
  }

  /* BUTTONS */
  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--green);
    color: #000;
    font-weight: 700;
    font-family: 'Montserrat', sans-serif;
    padding: 0.75rem 1.5rem;
    border-radius: 500px;
    font-size: 0.9rem;
    transition: background 0.15s, transform 0.1s;
    white-space: nowrap;
    border: none;
    cursor: pointer;
  }
  .btn-primary:hover { background: var(--green-light); transform: scale(1.02); }
  .btn-primary:active { transform: scale(0.98); }

  .btn-sm { padding: 0.5rem 1.1rem; font-size: 0.8rem; }
  .btn-lg { padding: 1rem 2.2rem; font-size: 1rem; }

  .btn-ghost {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: transparent;
    color: var(--text);
    font-weight: 600;
    font-family: 'Montserrat', sans-serif;
    padding: 0.75rem 1.5rem;
    border-radius: 500px;
    font-size: 0.9rem;
    border: 1.5px solid rgba(255,255,255,0.18);
    transition: border-color 0.15s, background 0.15s;
  }
  .btn-ghost:hover { border-color: rgba(255,255,255,0.5); background: rgba(255,255,255,0.05); }

  /* EQUALIZER */
  .equalizer {
    display: flex;
    align-items: flex-end;
    gap: 3px;
    height: 28px;
    margin-bottom: 1.25rem;
  }
  .equalizer span {
    display: block;
    width: 4px;
    background: var(--green);
    border-radius: 2px;
    transform-origin: bottom;
    animation: eq-bounce 1.2s ease-in-out infinite;
  }
  .equalizer span:nth-child(1) { height: 60%; animation-delay: 0s; }
  .equalizer span:nth-child(2) { height: 100%; animation-delay: 0.15s; }
  .equalizer span:nth-child(3) { height: 45%; animation-delay: 0.3s; }
  .equalizer span:nth-child(4) { height: 80%; animation-delay: 0.1s; }
  .equalizer span:nth-child(5) { height: 55%; animation-delay: 0.25s; }

  .equalizer--lg {
    height: 40px;
    gap: 4px;
    margin-bottom: 1.5rem;
    justify-content: center;
  }
  .equalizer--lg span { width: 6px; }

  @keyframes eq-bounce {
    0%, 100% { transform: scaleY(0.4); }
    50%       { transform: scaleY(1); }
  }

  /* HERO */
  .hero {
    padding: 120px clamp(1.5rem, 5vw, 4rem) 80px;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .hero-content {
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 380px;
    gap: 60px;
    align-items: center;
  }

  h1 {
    font-size: clamp(2.2rem, 5vw, 3.5rem);
    font-weight: 900;
    line-height: 1.1;
    letter-spacing: -1px;
    margin-bottom: 1.25rem;
  }

  .hero-sub {
    font-size: 1.05rem;
    color: var(--muted);
    max-width: 480px;
    margin-bottom: 2rem;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-bottom: 1.25rem;
  }

  .hero-note {
    font-size: 0.78rem;
    color: var(--subdued);
    font-weight: 500;
  }

  /* PHONE FRAME */
  .phone-frame {
    position: relative;
    width: fit-content;
    border-radius: 44px;
    overflow: hidden;
    box-shadow:
      0 0 0 2px rgba(255,255,255,0.08),
      0 0 0 10px #111,
      0 0 0 12px rgba(255,255,255,0.06),
      0 32px 80px rgba(0,0,0,0.7);
  }

  .phone-frame img {
    display: block;
    border-radius: 42px;
  }

  .phone-frame--sm {
    border-radius: 32px;
    box-shadow:
      0 0 0 2px rgba(255,255,255,0.07),
      0 0 0 8px #111,
      0 0 0 9px rgba(255,255,255,0.05),
      0 20px 50px rgba(0,0,0,0.6);
  }
  .phone-frame--sm img { border-radius: 30px; width: 200px; }

  /* glow behind hero phone */
  .hero-phone {
    display: flex;
    justify-content: center;
    position: relative;
  }
  .hero-phone::before {
    content: '';
    position: absolute;
    inset: 10% 5%;
    background: radial-gradient(ellipse at center, rgba(29,185,84,0.18) 0%, transparent 70%);
    pointer-events: none;
  }

  @media (max-width: 840px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .hero-sub { margin-left: auto; margin-right: auto; }
    .hero-actions { justify-content: center; }
    .hero-note { text-align: center; }
    .equalizer { justify-content: center; }
    .hero-phone { margin-top: 2rem; }
    .phone-frame img { width: 260px; height: auto; }
  }

  /* SECTION COMMON */
  .section-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 clamp(1.5rem, 5vw, 4rem);
  }

  section:not(.hero):not(.cta) { padding: 80px 0; }

  .section-title {
    font-size: clamp(1.6rem, 3.5vw, 2.4rem);
    font-weight: 800;
    letter-spacing: -0.5px;
    margin-bottom: 0.75rem;
    text-align: center;
  }

  .section-sub {
    font-size: 1rem;
    color: var(--muted);
    text-align: center;
    margin-bottom: 3rem;
    max-width: 560px;
    margin-left: auto;
    margin-right: auto;
  }

  /* FEATURES */
  .features { background: var(--surface); }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.25rem;
  }

  .feature-card {
    background: var(--raised);
    border: 1px solid rgba(255,255,255,0.06);
    border-radius: var(--radius);
    padding: 1.75rem 1.5rem;
    transition: border-color 0.2s, transform 0.2s;
  }
  .feature-card:hover {
    border-color: rgba(29,185,84,0.3);
    transform: translateY(-3px);
  }

  .feature-icon {
    color: var(--green);
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }

  .feature-card p {
    font-size: 0.875rem;
    color: var(--muted);
    line-height: 1.6;
  }

  /* SCREENSHOTS */
  .screenshots { overflow: hidden; }

  .screenshots-scroll {
    overflow-x: auto;
    padding: 0 clamp(1.5rem, 5vw, 4rem) 1rem;
    scrollbar-width: thin;
    scrollbar-color: var(--elevated) transparent;
  }
  .screenshots-scroll::-webkit-scrollbar { height: 4px; }
  .screenshots-scroll::-webkit-scrollbar-track { background: transparent; }
  .screenshots-scroll::-webkit-scrollbar-thumb { background: var(--elevated); border-radius: 2px; }

  .screenshots-track {
    display: flex;
    gap: 2rem;
    width: max-content;
    padding: 0.5rem 0 1.5rem;
    margin: 0 auto;
  }

  .screenshot-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .screenshot-label {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--muted);
    letter-spacing: 0.5px;
  }

  /* HOW IT WORKS */
  .how-it-works { background: var(--surface); }

  .steps {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    max-width: 600px;
    margin: 0 auto;
  }

  .step {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .step-number {
    flex-shrink: 0;
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--green);
    color: #000;
    font-weight: 800;
    font-size: 1.1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 2px;
  }

  .step-body h3 {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.3rem;
  }

  .step-body p {
    font-size: 0.9rem;
    color: var(--muted);
    line-height: 1.6;
  }

  /* CTA */
  .cta {
    padding: 100px 0;
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  .cta::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 50% 0%, rgba(29,185,84,0.12) 0%, transparent 60%);
    pointer-events: none;
  }

  .cta-inner {
    position: relative;
  }

  .cta h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 900;
    letter-spacing: -0.5px;
    margin-bottom: 1rem;
  }

  .cta p {
    font-size: 1rem;
    color: var(--muted);
    margin-bottom: 2rem;
    max-width: 440px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-note {
    margin-top: 1.25rem;
    font-size: 0.8rem;
    color: var(--subdued);
    font-weight: 500;
  }

  /* FOOTER */
  footer {
    border-top: 1px solid rgba(255,255,255,0.06);
    padding: 2.5rem 0;
  }

  .footer-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 clamp(1.5rem, 5vw, 4rem);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    text-align: center;
  }

  .footer-brand {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 700;
    font-size: 0.95rem;
  }

  .footer-disclaimer {
    font-size: 0.78rem;
    color: var(--subdued);
    max-width: 600px;
    line-height: 1.6;
  }

  .footer-links {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .footer-links a {
    font-size: 0.85rem;
    color: var(--muted);
    font-weight: 500;
    transition: color 0.15s;
  }
  .footer-links a:hover { color: var(--text); }

  .footer-copy {
    font-size: 0.75rem;
    color: var(--subdued);
  }
</style>

<script>
  document.querySelectorAll('.dl-apk').forEach(el => {
    el.addEventListener('click', async () => {
      const btn = el as HTMLButtonElement;
      const orig = btn.innerHTML;
      btn.disabled = true;
      btn.textContent = 'Fetching...';
      try {
        const res = await fetch('https://api.github.com/repos/mliem2k/playtivity/releases');
        const releases: { prerelease: boolean; assets: { name: string; browser_download_url: string }[] }[] = await res.json();
        const nightly = releases.find(r => r.prerelease);
        const apk = nightly?.assets?.find(a => a.name.endsWith('.apk'));
        if (!apk) throw new Error('APK not found');
        window.location.href = apk.browser_download_url;
        btn.innerHTML = orig;
      } catch {
        btn.textContent = 'Download failed';
      } finally {
        btn.disabled = false;
      }
    });
  });
</script>
